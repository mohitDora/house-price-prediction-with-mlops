# .dockerignore

.git
.gitignore
.dvc/
.venv/
env/
logs/
__pycache__/
*.pyc
*.egg-info/
mlruns/
temp/

# Ignore actual data and preprocessor/model files, as DVC will pull them inside the container
# This is crucial for small Docker images and for DVC to manage them.
data/raw_data.csv
data/processed_data.parquet
models/preprocessor.joblib
models/final_model.joblib

# DVC specific files (ignore, as they are not needed in the final image)
# except for the .dvc files themselves which Git tracks and are needed for dvc pull
*.dvc
.dvcignore